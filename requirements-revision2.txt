# Exact package versions used for the second revision (August 2026).
# The XGBoost and SHAP results reproduce the originally released values to four
# decimal places under these versions. RuleFit reproduces the released
# repeated-split mean R2 to within 0.003 (0.766 against 0.769), because the
# underlying tree-ensemble and coordinate-descent implementations changed
# between library releases; see Section 3.2 of the manuscript.
numpy==2.3.5
pandas==3.0.2
scipy==1.16.0
scikit-learn==1.8.0
xgboost==3.1.1
shap==0.51.0
matplotlib==3.10.9
seaborn==0.13.0
lime==0.2.0.1
rulefit==0.3.1
openpyxl==3.1.5
python-docx==1.2.0
